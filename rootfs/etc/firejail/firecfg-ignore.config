# Exclude some programs from the default /etc/firejail/firecfg.config
# This allows you not to edit the default config and let upstream
# maintain it for us.
# To understand how this file is used, see:
# /etc/pacman.d/hooks/post-20-firejail-symlinks.hook
man # because I use nvim as MANPAGER
